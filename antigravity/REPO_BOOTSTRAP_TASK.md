# Repository Bootstrap Task

## Objective

Validate the supplied architecture-first source tree and prepare a reviewable WO-000 plan. Do not select/initialize a framework, install dependencies, provision cloud resources, or write product code.

## Checks

- Required root/docs/ADR/WO/spec/app/service/adapter/eval/agent/Antigravity paths exist.
- 18 substantive work orders exist with all required sections.
- Five ADRs exist and preserve model/domain/graph/local/human-authority decisions.
- No real PHI, secrets, credentials, generated patient-like production data, or external account identifiers.
- No instruction grants AI clinical authority or direct state/external mutation.
- Core cloud independence and Google adapter-only guidance are present.
- Git, review, release-gate, and completion-evidence rules are present.

## Deliverable

Create no files unless explicitly authorized after reporting validation. Propose exact bootstrap files/commands, stack decision options requiring ADR, risk register, reviewers, criterion evidence, and gate decision. Unknown founder choices are `FOUNDER DECISION REQUIRED`; clinical ambiguity is `REQUIRES CLINICAL DECISION`.

