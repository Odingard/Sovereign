# Synthetic Data Only

Only invented test people, organizations, identifiers, encounters, evidence, communications, and outcomes are allowed here. Never copy, transform, paraphrase, screenshot, or derive fixtures from a real patient record. Each fixture should declare `synthetic: true`, a generator/scenario version, and an obvious reserved/nonreal identifier namespace. Automated CI must scan and reject prohibited data patterns.

